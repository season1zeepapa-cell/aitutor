{
  "chapter_code": "PY-SF-14",
  "title": "솔트 없이 일방향 해시 함수 사용",
  "stage": "implementation",
  "category": "security_feature",
  "cwe": "CWE-759",
  "applies_to": [
    "Python"
  ],
  "source": {
    "doc": "Python 시큐어코딩 가이드(2023년 개정본)",
    "section": "제2장 제2절 보안기능 / 14. 솔트 없이 일방향 해시 함수 사용",
    "pages": "111-112"
  },
  "overview": "패스워드와 같이 중요정보를 저장할 경우 가변 길이 데이터를 고정된 크기의 해시값으로 변환해주는 일방향 해시함수를 이용해 저장할 수 있다.",
  "impact": "만약 중요정보를 솔트(Salt) 없이 일방향 해시함수를 사용해 저장한다면 공격자는 미리 계산된 레인보우 테이블을 이용해 해시값을 알아낼 수 있다.",
  "countermeasure": "패스워드와 같은 중요 정보를 저장할 경우 임의의 길이인 데이터를 고정된 크기의 해시값으로 변환해주는 일방향 해시함수를 이용하여 저장한다. 또한 솔트값은 사용자별로 유일하게 생성해야 하며, 이를 위해 사용자별 솔트 값을 별도로 저장하는 과정이 필요하다. 파이썬에서는 hashlib 라이브러리를 사용해 해시값을 생성할 수 있으며 salt 값은 os.urandom() 등 안전한 난수 생성 라이브러리를 사용하여 생성해야 한다.",
  "code_examples": [
    {
      "lang": "Python (hashlib)",
      "vulnerable": "import hashlib\n\ndef get_hash_from_pwd(pw):\n  # salt 없이 생성된 해시값은 강도가 약해 취약하다\n  h = hashlib.sha256(pw.encode())\n\n  return h.digest()",
      "safe": "import hashlib\nimport secrets\n\ndef get_hash_from_pwd(pw):\n  # 솔트 값을 사용하면 길이가 짧은 패스워드로도 고강도의 해시를 생성할 수 있다.\n  # 솔트 값은 사용자별로 유일하게 생성해야 하며, 패스워드와 함께 DB에 저장해야 한다\n  salt = secrets.token_hex(32)\n  h = hashlib.sha256(salt.encode() + pw.encode())\n\n  return h.digest(), salt",
      "note": "안전하지 않은 코드는 salt 없이 길이가 짧은 패스워드를 해시 함수에 전달해 원문이 공격자에 의해 쉽게 유추될 수 있다. 안전한 코드는 secrets.token_hex(32)로 솔트를 생성해 패스워드와 함께 해시함으로써 짧은 패스워드로도 강도 높은 해시값을 생성한다."
    }
  ]
}
