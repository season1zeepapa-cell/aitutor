{
  "chapter_code": "PY-IV-14",
  "title": "정수형 오버플로우",
  "stage": "implementation",
  "category": "input_validation",
  "cwe": "CWE-190",
  "applies_to": ["Python"],
  "source": {
    "doc": "Python 시큐어코딩 가이드(2023년 개정본)",
    "section": "제2장 제1절 입력데이터 검증 및 표현 / 14. 정수형 오버플로우",
    "pages": "61-63"
  },
  "overview": "정수형 오버플로우는 정수형 크기가 고정된 상태에서 변수가 저장할 수 있는 범위를 넘어선 값을 저장하려 할 때 실제 저장되는 값이 의도치 않게 아주 작은 수 또는 음수가 되어 프로그램이 예기치 않게 동작하게 되는 취약점이다. 파이썬 2.x에서는 int 타입 변수의 값이 표현 가능한 범위를 넘어서게 되면 자동으로 long으로 타입을 변경해 범위를 확장한다. 파이썬 3.x에서는 long 타입을 없애고 int 타입만 유지하되, 정수 타입의 자료형에 'Arbitrary-precision arithmetic' 방식을 사용해 오버플로우를 발생하지 않는다. 하지만 파이썬 3.x에서도 기존의 pydata stack을 사용하는 패키지를 사용할 때는 C언어와 동일하게 정수형 데이터가 처리되므로 오버플로우 발생에 유의해야 한다. 이처럼 언어 자체에서는 안정성을 보장하지만 특정 취약점에 취약한 패키지 또는 라이브러리를 사용하는 것에 주의해야 한다.",
  "impact": "특히 반복문 제어, 메모리 할당, 메모리 복사 등을 위한 조건으로 사용자가 제공하는 입력값을 사용하고 그 과정에서 정수형 오버플로우가 발생하는 경우 보안상 문제를 유발할 수 있다.",
  "countermeasure": "기본 파이썬 자료형을 사용하지 않고 패키지에서 제공하는 데이터 타입을 사용할 경우 해당 패키지에서 제공하는 데이터 타입의 표현 방식과 최대 크기를 반드시 확인해야 한다. numpy에서는 기본적으로 64비트 길이의 정수형 변수를 사용하며, 변수가 표현할 수 없는 큰 크기의 숫자는 문자열 형식(object)으로 변환하는 기능을 제공한다. 하지만 64비트를 넘어서는 크기의 숫자는 제대로 처리하지 못한다. 따라서 변수에 값 할당 전에 반드시 변수의 최소 및 최대값을 확인하고 범위를 넘어서는 값을 할당하지 않는지 테스트해야 한다.",
  "code_examples": [
    {
      "lang": "Python (NumPy)",
      "vulnerable": "import numpy as np\n\ndef handle_data(number, pow):\n res = np.power(number, pow, dtype=np.int64)\n # 64비트를 넘어서는 숫자와 지수가 입력될 경우 오버플로우가 발생해 결과값이 0이 된다\n return res",
      "safe": "import numpy as np\n\nMAX_NUMBER = np.iinfo(np.int64).max\nMIN_NUMBER = np.iinfo(np.int64).min\n\ndef handle_data(number, pow):\n\n calculated = number ** pow\n # 파이썬 기본 자료형으로 큰 수를 계산한 후 이를 검사해 오버플로우 탐지\n if calculated > MAX_NUMBER or calculated < MIN_NUMBER:\n   # 오버플로우 탐지 시 비정상 종료를 나타내는 -1 값 반환\n   return -1\n\n res = np.power(number, pow, dtype=np.int64)\n return res",
      "note": "취약 코드는 거듭제곱을 계산해 반환하는 함수로, 계산 가능한 숫자에 대한 검증이 없어 에러는 발생하지 않지만 64비트를 넘어서는 숫자·지수가 입력되면 오버플로우로 결과값이 0이 되어 반환값을 처리하는 함수에서 예기치 않은 오류가 발생할 수 있다. 안전 코드는 오버플로우가 발생하지 않는 파이썬 기본 자료형으로 먼저 계산한 뒤 그 값을 np.int64의 최소·최대값(np.iinfo)과 비교해 오버플로우 여부를 확인하고, 범위를 벗어나면 -1을 반환한다."
    }
  ]
}
