# REBUILD50 — 자료 라이브러리 패널 바텀시트 전환

> **작성**: 2026-06-15 KST
> **트리거**: 사용자 요청 — "자료 라이브러리 모달을 하단에서 나오도록, 좌우 사이즈에 맞춰"
> **범위**: `src/components/LibraryFab.jsx` 패널 컨테이너
> **결과**: 우측 드로어 → 하단 바텀시트(전체 너비)

---

## §0. 변경

| 항목 | Before | After |
|------|--------|-------|
| 출현 | 우측 드로어 `flex justify-end` | 하단 `flex items-end` |
| 너비 | `max-w-md` | **`w-full` (좌우 전체)** |
| 높이 | `h-full` | `max-h-[85vh]` (내용만큼, 최대 85vh) |
| 모양 | 직각 | `rounded-t-2xl` + 드래그 핸들 바 |

- 헤더·검색 상단 고정, 본문 `flex-1 overflow-y-auto` 스크롤 유지.
- 플로팅 버튼(우측 하단 `bottom-32`)·아코디언·코드/점프 기능 모두 그대로.

## §1. 검증 / 영향

| 항목 | 결과 |
|------|------|
| 빌드 | ✅ 통과 |
| 영향 | UI 레이아웃만 (데이터·로직 무변경) |
| 배포 | 🟡 src 변경 → Cloud Run 재배포 |

---

**완료 일시**: 2026-06-15 KST
**연관 문서**: REBUILD45(플로팅 메뉴), REBUILD46(정렬·아코디언)
