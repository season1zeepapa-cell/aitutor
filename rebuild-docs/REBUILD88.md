# REBUILD88 — composite(복합서술형) 구현단계 확충: 52 → 93, 전 69챕터 커버

> 2026-07-04. 선행: REBUILD87(진단코드 문항화·DSG-EH-01 blank). REBUILD85 §6-2 과제 완료.

## §1. 배경

composite 는 52문항·28챕터로 커버리지 최저(41%)였다 — 설계 20챕터는 전부 있었으나 **구현단계는 49 중 8챕터만** 보유.
빠진 구현 41챕터에 각 1문항(8점 진단보고서형)을 생성해 전 69챕터 커버로 확충.

## §2. 생성 방식 — `scripts/build-kisa-composite-impl.mjs` (신규)

기존 IMP-IV-01 composite 규격을 그대로 따른다:
- **산출물 3종**: 보안요구사항명세서(REQ-xx-01/02) · 개발가이드(안전 구현 규칙) · 개발산출물(취약 소스코드)
- **취약 코드·원문 해설(note)은 kisa-library.json 진단가이드 원문에서 자동 주입** — 코드 창작 없음(원문 그대로)
- **rubric 3항목(3/3/2점)**: ①결함 진단 ②판정·현황 ③개선방안 — `required_keywords` 채점(scoreComposite 비율 배분)
- 챕터별 진단포인트·요구사항·가이드 규칙·rubric 키워드 41세트는 스크립트 내 SPEC 테이블에 수작성
  (키워드는 REBUILD82 §4 교훈대로 모범 보고서에 자연히 등장할 짧은 핵심어)
- weakness_code `IMP-XX-NN-C1`, 난이도 상, language 는 코드 언어 매핑(C→etc).

## §3. 검증

- **채점기 자가검증**: 41문항 전수 — 전 키워드 포함 모범 보고서 **100점 41/41**, 무관 보고서 **0점 41/41**.
- DB 표본(IMP-TS-01·CE-05·AA-02): artifacts 3·rubric 3 정상.
- 적재: 삽입 41 · 스킵 0 (weakness_code 존재 검사, 재실행 안전).

## §4. 결과

| | 이전 | 이후 |
|---|-----:|-----:|
| composite 문항 | 52 (설계44+구현8) | **93 (설계44+구현49)** |
| 커버 챕터 | 28/69 | **69/69** |
| 문항 DB 총계 | 2,791 | **2,832** |

부수 효과: 실전 모의 실기(composite 2문항 슬롯)의 출제 다양성 확대.

## §5. 참고

- DB·시드 데이터만 변경 → 배포 불필요 (드릴·모의시험은 DB 실시간 조회).
- 시드: `kisa-module/seed/composite-impl-expansion.json` (재생성 시 스크립트 재실행).
- REBUILD85 §6 남은 과제 중 1(blank 공백)·2(composite 확충) 완료 — 남은 것: 소수 카테고리(objective/shortessay 9·10번째) 증설, codeid 는 상한 도달.
